# Angular Boilerplate

Basic setup for starting an angular based web application
 
Run **'gulp'** in terminal to get up an running on **localhost:3000**. See Gulp file for specifications.

API access is f*****. Go to http://clients.envisage.dk/appear/i-log/php/keygen.php. Create componentID, timestamp and signature and paste it into url below, instead of '************'

http://localhost:8888/?componentID=**********&timestamp=*************&signature=********************

open chrome from terminal using 'open -a Google\ Chrome --args --disable-web-security'

got http://localhost:8888/?componentID=**********&timestamp=*************&signature=******************** ( with your pasted values instead of the *'s)

And you are up and running.
